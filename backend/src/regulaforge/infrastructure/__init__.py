"""Infrastructure layer package.

Implements adapters for database access, message brokering,
caching, external APIs, and monitoring. These are the concrete
implementations of the port interfaces defined in the domain
and application layers.
"""
